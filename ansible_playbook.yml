---
- name: Check disk space usage
  hosts: all
  gather_facts: no
  tasks:
    - name: Run df -h command
      command: df -h
      register: disk_usage

    - name: Display disk usage
      debug:
        var: disk_usage.stdout_lines

